<%= form_with(model: @shop, url: scrape_task_path(@shop), method: :post) do |form| %>
    <div class="field">
      <%= form.label :url %>
      <%= form.text_field :url %>
    </div>
    <div class="actions">
      <%= form.submit 'Scrape' %>
    </div>
<% end %>
  