<!DOCTYPE html>
<html>
  <head>
    <title>OtakoiGourmetmap</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <%= render partial: 'shared/header_logged_in' if logged_in? %>
  <%= render partial: 'shared/header_logged_out' unless logged_in? %>
  
    <%= notice %>
    <%= alert %>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
